name: Create Tag

on:
  push:
    branches:
      - main

jobs:
  create-tag:
    runs-on: ubuntu-latest
    steps:
     - name: Checkout Code
       uses: actions/checkout@v2
     - name: Setup PHP
       uses: shivammathur/setup-php@v2
       with: 
          php-version: '7.2'
     - name: PHP Work
       id: set-output
       env: 
        COMMITTEXT: "${{ github.event.head_commit.message }}"
       run: |
          php bin/get-commit-message.php
# echo "::set-output name=release_body::$(cat release.txt)"
# OUTPUT=$(php bin/get-commit-message.php)
# echo ${OUTPUT}
# echo "after"
# echo "::set-output name=release_body::${OUTPUT}"
     - name: Set tag
       id: set-tag
       run:  echo "::set-output name=tag_name::$(sh cut.sh)"
     - name: Output tags
       continue-on-error: true
       run: ${{ steps.set-tag.outputs.tag_name }}
     - name: Create tag
       uses: actions/github-script@v3
       env:
          TAG: ${{ steps.set-tag.outputs.tag_name }}
       with:
          github-token: ${{ github.token }}
          script: |
           // console.log( process.env)
            github.git.createRef({
              owner: context.repo.owner,
              repo: context.repo.repo,
             ref: "refs/tags/${{ steps.set-tag.outputs.tag_name }}",
              sha: context.sha
            })
     - name: Create release
       uses: ncipollo/release-action@v1
       with:
        tag: ${{ steps.set-tag.outputs.tag_name }}
        #commit: GITHUB_REF
        # body: ${{ steps.set-output.outputs.release_body }}
        bodyFile: './release_notes.txt'
        token: ${{ secrets.GITHUB_TOKEN }}